# Failure-Prediction-in-Predictive-Maintenance-Using-ML
Explored and compared machine learning models for predictive maintenance to predict machine failures. Used XGBoost, Random Forest, and Bagging Classifiers (Decision Tree, Logistic Regression). Evaluated model performance to identify machine failures effectively using dataset features.
